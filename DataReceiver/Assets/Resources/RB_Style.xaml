<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="RB_Navigation" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="Transparent" CornerRadius="5" x:Name="El_bg" Margin="3">
                        <StackPanel Orientation="Horizontal" Height="50" >
                            <Border x:Name="Indicator" 
                        Height="50" 
                        Width="5" 
                        CornerRadius="5,0,0,5" 
                        Background="#7B68EE" 
                        Margin="2,5,10,5" 
                        Visibility="Hidden"
                        VerticalAlignment="Center"/>
                            <ContentControl x:Name="con_fr">
                                <ContentPresenter></ContentPresenter>
                            </ContentControl>
                        </StackPanel>
                    </Border>


                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Indicator" Property="Background" Value="#7B68EE"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="El_bg" Property="Background" Value="#DCDCDC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    
    
    
</ResourceDictionary>