<Window x:Class="DataReceiver.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataReceiver.Views"
        xmlns:vm="clr-namespace:DataReceiver.ViewModels"
        mc:Ignorable="d"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="MainView" Height="650" Width="1200">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding WindowLoadedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition MaxHeight="30"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Transparent">
            <Border Background="{DynamicResource border_background}" BorderThickness="1,1,1,0" BorderBrush="{DynamicResource border_brush}" CornerRadius="5,5,0,0">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <!--function button-->
                    <Grid Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <Ellipse Style="{StaticResource ellipse_style}" Fill="DarkOrange" >
                            </Ellipse>
                        </Grid>

                        <!--<Button Style="{StaticResource style}"-->

                        <Grid Grid.Column="1">
                            <Ellipse Width="18" Height="18" Fill="Green" Margin="0,0,15,0"/>
                        </Grid>

                        <Grid Grid.Column="2">
                            <Ellipse Width="18" Height="18" Fill="PaleVioletRed" Margin="0,0,15,0"/>
                        </Grid>

                    </Grid>

                    <!--icon-->
                    <Grid Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="17,0,10,0" VerticalAlignment="Center" >
                            <Path Data="{StaticResource appIcon_geom}" 
                                  Stroke="{DynamicResource icon_foreground}" 
                                  Stretch="Uniform" Fill="{DynamicResource icon_foreground}" 
                                  Width="17" 
                                  Height="17"/>
                            <TextBlock Text="This is a title" Margin="10,0,10,0" FontSize="14" FontWeight="Medium" FontFamily="Calibri"/>
                        </StackPanel>
                    </Grid>
                </Grid>

            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!--sider bar-->
            <ListBox Grid.Column="0" ItemsSource="{Binding NaviList}" SelectionMode="Single"/>
            
            <!--main region-->
            <ContentControl Grid.Column="1" x:Name="MainFrameContainer"/>
        </Grid>

        <!--bottom bar-->
        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Connected!" Margin="10,0,0,0" />
            </Grid>

            <Grid Grid.Column="1">
                <StackPanel  HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Width="auto">
                    <hc:Badge Text="1" BadgeMargin="0,5,0,-10" Height="20" FontSize="11" Foreground="Black" Background="PaleVioletRed">
                        <Path Data="{StaticResource home_geom}" Stroke="{DynamicResource badge_forground}" Stretch="Uniform" Fill="{DynamicResource badge_forground}" Width="15" Height="15" Margin="10,2,15,2"/>
                    </hc:Badge>

                    <hc:Badge Text="1" BadgeMargin="0,5,0,-10" Height="20" FontSize="11" Foreground="Black" Background="PaleVioletRed">
                        <Path Data="{StaticResource data_geom}" Stroke="{DynamicResource badge_forground}" Stretch="Uniform" Fill="{DynamicResource badge_forground}" Width="15" Height="15" Margin="10,2,15,2"/>
                    </hc:Badge>

                    <hc:Badge Text="1" BadgeMargin="0,5,0,-10" Height="20" FontSize="11" Foreground="Black" Background="PaleVioletRed">
                        <Path Data="{StaticResource socket_geom}" Stroke="{DynamicResource badge_forground}" Stretch="Uniform" Fill="{DynamicResource badge_forground}" Width="15" Height="15" Margin="10,2,15,2"/>
                    </hc:Badge>

                    <hc:Badge Text="1" BadgeMargin="0,5,0,-10" Height="20" FontSize="11" Foreground="Black" Background="PaleVioletRed">
                        <Path Data="{StaticResource data_geom}" Stroke="{DynamicResource badge_forground}" Stretch="Uniform" Fill="{DynamicResource badge_forground}" Width="15" Height="15" Margin="10,2,15,2"/>
                    </hc:Badge>
                </StackPanel>
            </Grid>

        </Grid>
    </Grid>
</Window>
